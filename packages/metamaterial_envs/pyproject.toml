[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["metamaterial_envs"]

[project]
name = "metamaterial_envs"
version = "0.0.1"
dependencies = [
  "gymnasium",
  "pygame>=2.1.3",
]
